spring:
  datasource:
    url: jdbc:mysql://localhost:3306/login?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    open-in-view: true
server:
    port: 8087

